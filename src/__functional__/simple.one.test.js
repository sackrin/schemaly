describe('Simple Functional Test One', () => {
  it ('can create a schema as expected');

  it ('can hydrate, validate and dump as expected');

  it ('can filter out invalid values');

  it ('can respond to changing scope and roles');
});
